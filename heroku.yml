# setup:
#   addons:
#     - plan: heroku-postgresql
#       as: DATABASE
#   config:
#     S3_BUCKET: my-example-bucket
build:
  docker:
    userapp: user-app/Dockerfile
    courseapp: course-app/Dockerfile
    config:
      NODE_ENV: production
      APPID: 1111
    
release:
  image: userapp
  command:
    - npm run app
run:
  userapp: npm run app
  courseapp: npm run app
  # asset-syncer:
  #   command:
  #     - python asset-syncer.py
  #   image: worker